#include <iostream>
#include <vector>
using namespace std;
int main() {
	int n;
	cin >> n;
	vector<int> v;
	for(int i = 0; i < n; i++){
		int temp = 0;
		cin >> temp;
		v.push_back(temp);
	}
	int num;
	cin >> num;
	int pos = 0;
	cin >> pos;
	v.insert(v.begin() + pos, num);
	for(auto i: v){
		cout << i << " ";
	}
}
